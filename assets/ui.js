// assets/ui.js
// Deprecated loader — all pages now load their needed scripts directly.
// This file is kept only for backwards compatibility.
// No pages should reference this file anymore.

(function () {
  if (typeof window === 'undefined' || typeof document === 'undefined') return;
  console.warn('assets/ui.js is deprecated and no longer needed. Remove this script tag.');
})();
