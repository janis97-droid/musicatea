# وديع الصافي / Wadih El Safi

Upload the main portrait here as:

- `portrait.webp` preferred
- or `portrait.jpg`

Optional extra images:

- `stage-1.webp`
- `oud-1.webp`
- `archive-1.webp`

Connect in `data/character-images.js` with:

```js
"wadih-el-safi"
```

Note: history aliases may also use `wadih-al-safi`; keep this folder name aligned with the current character page slug.
